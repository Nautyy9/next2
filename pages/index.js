export default function Home() {
  return (
    <div className="">
      index page
    </div>
  )
}

//npm start run the build app(.next) to the browser
//npm run dev run the app in dev mode which update data on every change/render
//npm run build will built the (.next) folder which is necessary for production build to serve upto webs or host
