import React from 'react'

function another() {
  return (<>
    <div>This is another file in docs having name Another</div>
    <p>So with jsx we must name the function or class with same letter for typing convention in url i.e, Another !== another for function or class name(Another) but Another === another for function and class name (another)</p>
    </>
  )
}

export default another